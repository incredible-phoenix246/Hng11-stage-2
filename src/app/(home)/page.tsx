import React from "react";
import { HeroSection, GranteeSection, GallerySection } from "./(modules)";

const page = () => {
  return (
    <>
      <HeroSection />
      <GranteeSection />
      <GallerySection />
    </>
  );
};

export default page;
